	/**  Raccourcis Clavier  **/

/// Autocomplétion de commande
=> Tapez le début d'une commande(ex: « da »)
// If many name starts the same:
=> Tapez deux fois sur Tabulation
  ->  Affiche « dash » et « date »
// If only one name starts like this:
=> Tapez une fois sur Tabulation
  -> La commande se complète

/// Shortcuts
// Espace
`Passer à la PAGE SUIVANTE`
// Entré
`Aller à la LIGNE SUIVANTE`

// q 
`QUIT processus`
// CTRL + C
`ARRÊTE LE PROCESSUS en cours`
// Ctrl + D (= cmd « exit » )
`EXIT app: Envoie le message EOF (End Of File) à la console
Ferme la console si la ligne de commande est vide`

// Ctrl + L	(= cmd « clear » )
`EFFACE le contenu de la console`
// Ctrl + U
`SUPPRIME/COUPE tout ce qui se trouve à gauche du curseur`
// Ctrl + K
`SUPPRIME/COUPE tout ce qui se trouve à droite du curseur`
// Ctrl + W
`SUPPRIME/COUPE le premier mot situé à gauche du curseur`
// Ctrl + Y
`COLLE le texte supprimé avec « Ctrl + U » , « Ctrl + K »  ou « Ctrl + W »`

// Shift + PgUp (comme la molette de souris, en mode graphique uniquement)
`REMONTER dans les messages envoyés par la console`
// Shift + PgDown
`REDESCENDRE dans les messages envoyés par la console`

// Ctrl + A (touche « Home »)
`Ramène le curseur au début de la commande`
// Ctrl + E (touche « End »)
`Ramène le curseur à la fin de la ligne de commandes`

// Ctrl + R
`RECHERCHE parmi les commandes précédentes`

